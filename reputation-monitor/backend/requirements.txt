fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
sqlalchemy==2.0.29
alembic==1.13.1
psycopg2-binary==2.9.9
asyncpg==0.29.0
aiosqlite==0.20.0
celery==5.3.6
redis==5.0.3
transformers==4.40.0
torch==2.2.2
sentencepiece==0.2.0
langdetect==1.0.9
deep-translator==1.11.4
emoji==2.11.0
networkx==3.3
google-api-python-client==2.127.0
tweepy==4.14.0
instagrapi==2.1.2
pydantic-settings==2.2.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
slowapi==0.1.9
httpx==0.27.0
python-multipart==0.0.9
pytest==8.2.2
pytest-asyncio==0.23.7

# ---------------------------------------------------------------------------
# NC (Narrative Control) module
# ---------------------------------------------------------------------------
# Core narrative clustering uses scikit-learn (TF-IDF + DBSCAN). If absent, the
# module falls back to a dependency-free hashing-TF-IDF + greedy cosine clusterer,
# so this is recommended but not strictly required.
scikit-learn==1.4.2
numpy==1.26.4

# Optional model-enrichment layers. Each NC service degrades gracefully when its
# dependency is not installed (transcription/OCR/transformer-toxicity simply
# contribute no signal rather than failing). Install to enable full pipeline:
#   openai-whisper>=20231117      # transcript_service (Whisper) — needs ffmpeg
#   faster-whisper>=1.0.0         # PREFERRED transcript backend (large-v3) — CTranslate2
#   yt-dlp>=2024.4.9              # audio extraction for transcription
#   easyocr>=1.7.1               # ocr_service (thumbnail OCR) — needs torch
#   detoxify>=0.5.2              # toxicity_service transformer layer
#   sentence-transformers>=2.7.0 # narrative_service semantic embeddings
#                                 #   preferred model: intfloat/multilingual-e5-large
#                                 #   fallback: paraphrase-multilingual-mpnet-base-v2
#   faiss-cpu>=1.8.0             # narrative_service vector similarity search
